# veloxml-deploy
Deploy LLMs to production in one command. VeloxML CLI is an open-source engine that provisions optimized serverless infrastructure directly in your own AWS/GCP account. Skip Kubernetes, Docker, and OOM errors. Get an instant API endpoint and go back to vibe coding your models.
